Rewrite the academic paper below as a clear, self-contained explainer for first-time readers: motivated newcomers to the field — undergraduate-level in general quantitative reasoning (algebra, basic statistics, and standard notation for their broad domain), but with no prior exposure to this specific paper's subfield or its specialized vocabulary.

Follow these requirements exactly:

1. Structure
Reorganize the content into logical sections with clear Markdown headers (##, ###). Prioritize a structure that builds understanding step by step over mirroring the original paper's section order.

2. Concept explanations
For every key concept, method, or result, explain the intuition in plain language before introducing field-specific notation or terminology. Define any term, symbol, or abbreviation the first time it appears — never assume prior familiarity with this subfield's conventions, regardless of the paper's domain.

3. Mathematical and technical notation
Where the paper uses mathematical notation, render it in LaTeX, using $$ ... $$ for block equations and $ ... $ for inline expressions, compatible with MathJax/KaTeX rendering. Always wrap mathematical symbols and Greek letters (π, θ, α, β, Σ, etc.) in LaTeX math delimiters ($...$), never as plain Unicode text. Every non-trivial equation or formal expression must be followed by one to two sentences explaining what it computes and why it matters in context — the notation alone is never sufficient. If the paper is primarily non-mathematical (e.g. relies on qualitative methodology, experimental protocol, or descriptive results), apply the same standard to its technical terms and procedures instead.

4. Figures
Where the source paper references a figure or diagram, embed it with standard Markdown image syntax (![caption](path)), preserving the original filename. Immediately follow each image with an explanation of what it shows and how it supports the surrounding argument. Do not use this syntax for tables.

5. Tables
Reproduce all tables as clean Markdown tables — never as images.

6. Original supplementary material
Where it would meaningfully speed up a beginner's understanding, you may add your own simplified diagrams (described in words or simple Markdown/ASCII form) or summary tables not present in the original paper.

7. Tone
Write in a precise, professional, academic-adjacent register. Avoid emojis, informal asides, rhetorical filler ("let's dive in," "now let's explore"), and conversational transitions. The result should read as a polished, standalone technical document — not a tutorial or blog post.

8. Output
Return only the finished Markdown document. Do not include a preamble, summary of your approach, or any commentary about the rewriting process itself.